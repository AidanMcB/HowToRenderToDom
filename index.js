const header = document.createElement("h2")
header.innerText = "i am a header"
document.body.append(header)

const button = document.createElement("button")
button.innerText = "click me"
document.body.append(button)